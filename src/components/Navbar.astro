---

const path = Astro.url.pathname.replace(/\/$/, "") || "/";
---

<header class="sticky top-0 z-50 border-b border-line/70 bg-ink/90 backdrop-blur-sm">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-5 sm:px-10">
    <a
      href="/"
      class="font-sans text-sm text-paper transition-colors hover:text-accent"
    >
      Naman
    </a>

    <input type="checkbox" id="nav-toggle" class="peer hidden" />
    <label
      for="nav-toggle"
      class="cursor-pointer text-sm lowercase text-paper sm:hidden"
      aria-label="Toggle navigation"
    >
      menu
    </label>

    <nav
      class="hidden gap-8 text-sm lowercase text-paper-dim peer-checked:flex peer-checked:flex-col peer-checked:gap-4 peer-checked:absolute peer-checked:left-0 peer-checked:right-0 peer-checked:top-full peer-checked:border-b peer-checked:border-line/70 peer-checked:bg-ink peer-checked:px-6 peer-checked:py-6 sm:static sm:flex sm:flex-row sm:border-none sm:bg-transparent sm:px-0 sm:py-0"
    >
      <details class="photos-dropdown relative">
        <summary class:list={[
          "flex cursor-pointer list-none items-center gap-2 transition-colors hover:text-accent",
          path.startsWith("/photos/") && "text-accent",
        ]}>
          Photos
          <svg class="country-chevron h-3 w-3 transition-transform" viewBox="0 0 12 12" fill="none" aria-hidden="true"><path d="m3 4.5 3 3 3-3" stroke="currentColor" stroke-width="1.2" /></svg>
        </summary>
        <div class="mt-3 min-w-40 rounded-[12px] border border-line bg-ink p-2 shadow-lg sm:absolute sm:left-0 sm:top-full">
          <a href="/photos/new-zealand/" aria-current={path === "/photos/new-zealand" ? "page" : undefined} class="block whitespace-nowrap rounded-lg px-4 py-2 transition-colors hover:text-accent focus-visible:text-accent">New Zealand Winter '26</a>
          <a href="/photos/new-york/" aria-current={path === "/photos/new-york" ? "page" : undefined} class="block whitespace-nowrap rounded-lg px-4 py-2 transition-colors hover:text-accent focus-visible:text-accent">New York City Summer '26</a>
          <a href="/photos/japan/" aria-current={path === "/photos/japan" ? "page" : undefined} class="block whitespace-nowrap rounded-lg px-4 py-2 transition-colors hover:text-accent focus-visible:text-accent">Japan Winter '25</a>
        </div>
      </details>
      <a
        href="/moving/"
        class:list={[
          "transition-colors hover:text-accent",
          path === "/moving" && "text-accent",
        ]}
      >
        Moving
      </a>
      <a
        href="/about/"
        class:list={[
          "transition-colors hover:text-accent",
          path === "/about" && "text-accent",
        ]}
      >
        Working
      </a>

    </nav>
  </div>
</header>

<style>
  summary::-webkit-details-marker { display: none; }
  details[open] .country-chevron { transform: rotate(180deg); }
</style>
<script>
  const countryDropdown = document.querySelector<HTMLDetailsElement>(".photos-dropdown");
  document.addEventListener("click", (event) => {
    if (event.target instanceof Node && !countryDropdown?.contains(event.target)) {
      countryDropdown?.removeAttribute("open");
    }
  });
  countryDropdown?.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      countryDropdown.open = false;
      countryDropdown.querySelector("summary")?.focus();
    }
  });
</script>
